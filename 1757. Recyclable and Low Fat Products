import pandas as pd

def find_products(products: pd.DataFrame) -> pd.DataFrame:
   product_df = products[(products["low_fats"].str.lower() == 'y') &
    (products["recyclable"].str.lower( )=='y')]
   return product_df [["product_id"]]
